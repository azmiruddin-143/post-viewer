const loading = () => {
    return (
        <div className="text-center flex justify-center items-center text-red-700 h-screen">
             Loading...
        </div>
    );
};

export default loading;